// Numbers

const integer1 = 10;
const integer2 = 100;
const integer3 = 1000;
const integer4 = -1;

// Sum +
const sum = integer1 + integer3;
console.log( `Sum: ${sum}` );

// Subtraction -
const subtraction = integer3 - integer1;
console.log( `Subtraction: ${subtraction}` );

// Multiplication *
const multiplication = integer1 * integer2;
console.log( `Multiplication: ${multiplication}` );

// Division /
const division = integer3 / integer1;
console.log( `Division: ${division}` );